'use client';
import { useLocale } from '@/components/LocaleProvider';
const services=['CN2 Community','CN2 Forum','CN2 Identity','GPTWork','chat2api','QNBot','FDEX','Aipany'];
export default function StatusPage(){const{locale}=useLocale();const cn=locale==='zh-CN';const ja=locale==='ja';return <section className="shell page-section"><div className="page-hero"><span className="eyebrow">SERVICE STATUS</span><h1>{cn?'状态中心':ja?'ステータス':'Status'}</h1><p>{cn?'统一展示社区与各产品的服务状态、健康检查、版本和事件。当前基础站点先显示接入状态，生产部署后再连接真实健康检查。':ja?'コミュニティと各製品の稼働状況、ヘルスチェック、バージョン、インシデントを一元表示します。':'A shared view of community and product health, versions and incidents. Live health checks will connect as production services are deployed.'}</p></div><div className="status-list">{services.map((service,index)=><article key={service}><span className={index<2?'status-dot preview':'status-dot planned'}/><b>{service}</b><em>{index<2?(cn?'基础已接入':ja?'基盤接続済み':'Foundation connected'):(cn?'待服务接入':ja?'サービス接続予定':'Service integration planned')}</em></article>)}</div></section>}
