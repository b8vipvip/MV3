const services = ['CN2 Community', 'CN2 Forum', 'CN2 Identity', 'GPTWork', 'chat2api', 'QNBot', 'FDEX', 'Aipany'];
export default function StatusPage() { return <section className="shell page-section"><div className="page-hero"><span className="eyebrow">SERVICE STATUS</span><h1>状态中心</h1><p>第一版状态页建立统一展示结构。生产部署后将接入各产品健康检查、发布版本和事件记录。</p></div><div className="status-list">{services.map((service, index) => <article key={service}><span className={index < 2 ? 'status-dot preview' : 'status-dot planned'} /><b>{service}</b><em>{index < 2 ? 'Preview' : 'Integration planned'}</em></article>)}</div></section>; }
